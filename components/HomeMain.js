import React from "react";

const HomeMain = () => {
  return (
    <div
      className="h-[75vh] bg-gray-500 bg-opacity-40 flex items-center justify-center"
      style={{ backdropFilter: "blur(6px)" }}
    >
      HomeMain
    </div>
  );
};

export default HomeMain;
